---
title: "Dynamic Ad Campaign"
api: "POST /meta/create_campaign"
description: "This endpoint is used to create a campaign for conversion dynamic ads. Conversion ads are designed to drive users to take specific actions, such as completing a purchase, downloading an app, or signing up for a service."
---

import CampaignParams from "/snippets/create_campaign_params.mdx";
import CampaignTargeting from "/snippets/create_campaign_targeting.mdx";
import CampaignResponse from "/snippets/create_campaign_response.mdx";

## Request Body

<CampaignParams />

<CampaignTargeting />

### Dynamic Ads Parameters

<ParamField body="campaignType" type="string" required>
	For Conversions Goal, campaignType is `CONVERSIONS`
</ParamField>

<ParamField body="images" type="array">
	<Expandable title="Image Assets Array">
		<ParamField body="url" type="string" required>
			URL for image asset
		</ParamField>
	</Expandable>

</ParamField>

<ParamField body="videos" type="array">
	<Expandable title="Video Assets Array">
		<ParamField body="video_id" type="string" required>
			Facebook video ID, get facebook video id from [here](/api-reference/endpoint/meta/get_ad_account_media)
		</ParamField>
		<ParamField body="thumbnail_url" type="string" required>
			URL for video thumbnail image, get video thumbnail url from [here](/api-reference/endpoint/meta/get_ad_account_media)
		</ParamField>
	</Expandable>

</ParamField>

<ParamField body="primaryText" type="array">
	<Expandable title="Primary Text Array">
		<ParamField body="text" type="string" required>
			Main ad copy text
		</ParamField>
	</Expandable>

</ParamField>

<ParamField body="headline1" type="array">
	<Expandable title="Headline Variations Array">
		<ParamField body="text" type="string" required>
			Headline text variation
		</ParamField>
	</Expandable>

</ParamField>

<ParamField body="pixelId" type="string" required>
	Facebook Pixel ID for conversion tracking
</ParamField>

<ParamField body="conversionGoal" type="string" required>
	Campaign conversion goal type, get the list of conversion goals from
	[here](/api-reference/endpoint/meta/get_custom_conversions), use
	`CONVERSIONS` in campaignType to get the list of conversion goals.
</ParamField>

## Response

<CampaignResponse />
